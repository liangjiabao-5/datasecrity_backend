# Migrations

This phase includes SQLAlchemy models and a `flask init-db` bootstrap command.
Install `Flask-Migrate` and run Alembic migrations in project environments that
require versioned database change scripts.
